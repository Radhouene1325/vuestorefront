import { FetchResult } from "@apollo/client/core";
import type { CustomHeaders } from "@vue-storefront/magento-types";
import { ResetPasswordMutation, ResetPasswordMutationVariables } from "@vue-storefront/magento-types";
import { Context } from "../../types/context";
/**
 * Resets a user's password
 * @param context VSF Context
 * @param input Params used to reset a user's password
 * @param customHeaders (optional) - custom headers that extends the default headers
 */
export default function resetPassword(context: Context, input: ResetPasswordMutationVariables, customHeaders?: CustomHeaders): Promise<FetchResult<ResetPasswordMutation>>;
//# sourceMappingURL=index.d.ts.map