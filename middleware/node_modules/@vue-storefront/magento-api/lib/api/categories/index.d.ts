import { ApolloQueryResult } from "@apollo/client/core";
import type { CustomHeaders, Query } from "@vue-storefront/magento-types";
import { CustomQuery, QueryCategoriesArgs } from "@vue-storefront/magento-types";
import { Context } from "../../types/context";
/**
 * Fetches categories.
 *
 * @param context Context
 * @param params
 * @param [customQuery] (optional) - custom GraphQL query that extends the default query
 * @param customHeaders (optional) - custom headers that extends the default headers
 */
export default function categories(context: Context, params: QueryCategoriesArgs, customQuery?: CustomQuery, customHeaders?: CustomHeaders): Promise<ApolloQueryResult<Query["categories"]>>;
//# sourceMappingURL=index.d.ts.map