import { FetchResult } from "@apollo/client/core";
import type { CustomHeaders } from "@vue-storefront/magento-types";
import { PlaceOrderInput, PlaceOrderMutation } from "@vue-storefront/magento-types";
import { Context } from "../../types/context";
/**
 * Places an order for received cart.
 *
 * @param context VSF Context
 * @param input the order's input, containing the cart's ID
 * @param customHeaders (optional) - custom headers that extends the default headers
 */
export default function placeOrder(context: Context, input: PlaceOrderInput, customHeaders?: CustomHeaders): Promise<FetchResult<PlaceOrderMutation>>;
//# sourceMappingURL=index.d.ts.map