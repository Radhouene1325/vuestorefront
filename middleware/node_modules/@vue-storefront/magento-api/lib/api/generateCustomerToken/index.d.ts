import { FetchResult } from "@apollo/client/core";
import { CustomQuery, GenerateCustomerTokenMutation } from "@vue-storefront/magento-types";
import type { CustomHeaders } from "@vue-storefront/magento-types";
import { Context } from "../../types/context";
/**
 * Logs in the customer based on provided username and password. To override the default query, use the `generateCustomerToken` query key.
 */
declare const _default: (context: Context, params: {
    email: string;
    password: string;
    recaptchaToken: string;
}, customQuery?: CustomQuery, customHeaders?: CustomHeaders) => Promise<FetchResult<GenerateCustomerTokenMutation>>;
export default _default;
//# sourceMappingURL=index.d.ts.map